version: 0.2

phases:
  install:
    commands:
    - "apt update"
    - "apt install emacs"
  build:
    commands:
      - mkdir calendars
      - echo "Executing calendar export script..."
      - emacs --batch -Q -l export-calendars.el
artifacts:
  files:
    - "calendars/*"